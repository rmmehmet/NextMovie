# Database
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/nextmovie}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:123}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET:AL1LipfjjpK/BbiqTndgjWWTynbupuyJaJ9s8zE5MnI=}
jwt.expiration-ms=${JWT_EXPIRATION:86400000}

# TMDB
tmdb.api.key=${TMDB_API_KEY:c7040b454874f9b1b0daa55eb59f899b}

# Recommendation service
recommendation.service.url=${REC_SERVICE_URL:http://localhost:8001}

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Frontend URL (CORS)
app.frontend-url=${FRONTEND_URL:http://localhost:5173}

# Profile pictures upload path
app.upload-dir=${UPLOAD_DIR:uploads/profile-pictures/}

# Python recommendation service launcher
python.service.enabled=${PYTHON_SERVICE_ENABLED:true}
python.service.path=${PYTHON_SERVICE_PATH:../recommendation-service}
python.service.command=${PYTHON_COMMAND:python}